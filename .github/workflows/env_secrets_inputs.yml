name: Demo CI Env Secrets

on:
    push:
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo "Hello from the second workflow!"

            - name: Install
              env:
                  APP_ENV: dev
                  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
              run: echo "Environment $APP_ENV using DB password $DB_PASSWORD!"

    #         - name: Create demo file
    #           run: echo "Hello from CI Pipeline!" > demo.txt
    #                 echo "Demo file created!"

    #         - name: Upload Demo
    #           uses: actions/upload-artifact@v4
    #           with:
    #               name: build-artifacts-demo
    #               path: .

    # test:
    #     runs-on: ubuntu-latest
    #     needs: build

    #     steps:
    #         - name: Test job
    #           run: echo "Hello from the test job!"